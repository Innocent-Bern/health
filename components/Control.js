import styles from "../styles/Patient.module.css"
const Control = ({controlWidth}) => {
    
    return ( 
        <div style={controlWidth}  className={styles.control}>
        </div>
     );
}
 
export default Control;